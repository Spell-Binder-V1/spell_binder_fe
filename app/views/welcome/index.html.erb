<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
  <style>
    html, body {
      height: 100%;
      margin: 0;
      padding: 0;
    }

    body {
      background-image: url('https://cdn.discordapp.com/attachments/440548011014356993/1152387556852379678/Plains-Core-Set-2021-MtG-Art.jpg');
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      display: flex;
      flex-direction: column;
      justify-content: space-between;
      min-height: 100vh;
    }

    header {
      background-color: transparent;
      color: white;
      padding: 10px;
    }

    .main-content {
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      padding: 20px;
      flex-grow: 1;
    }

    .spellbinder-text {
      color: white;
      text-align: center;
      font-size: 50px;
      margin-bottom: 10px;
      letter-spacing: 6px;
      font-weight: lighter;
    }


    .search-container {
      text-align: center;
    }

    .search-input-container {
      display: flex;
      flex-direction: row;
      align-items: center;
      justify-content: center;
      border: 1px solid #ccc;
      border-radius: 5px;
      padding: 5px;
      background-color: white;
    }


    .search-input {
      width: 300px;
      border: none;
      padding: 5px;
    }

    .search-button {
      background-color: #356461;
      border: none;
      color: white;
      padding: 5px 15px;
      border-radius: 0 5px 5px 0;
      cursor: pointer;
      opacity: 0.9;
    }


    .nav-links {
      display: flex;
      align-items: center;
    }

    .nav-link {
      color: white;
      text-decoration: none;
      margin: 10px;
    }

    .login-button {
      background-color: transparent;
      border: none;
      color: white;
      cursor: pointer;
    }

    footer {
      background-color: transparent;
      color: rgb(81, 188, 142);
      text-align: center;
      padding: 10px;
      width: 100%;
    }
  </style>
</head>
<body>
  <header>
  </header>
  <main class="main-content">
    <div class="search-container">
      <h1 class="spellbinder-text">Spellbinder</h1>
      <%= form_tag(search_path, method: :get) do %>
        <div class="search-input-container">
          <%= text_field_tag :query, params[:q], placeholder: "Find Cards", class: "search-input" %>
          <%= submit_tag "Search", class: "search-button" %>
        </div>
      <% end %>
    </div>
    <div class="nav-links">
      <a href="/collections" class="nav-link">All Collections</a>
      <a href="/cards/random" class="nav-link">Random Card</a>
    </div>
  </main>
  <footer>
  </footer>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.min.js"></script>
</body>
</html>
